import 'package:flutter/material.dart';

import 'wtop_elements/wtop_elements.dart';

class WStatisticsBody extends StatelessWidget {
  const WStatisticsBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        WTopElements(),

      ],
    );
  }
}
